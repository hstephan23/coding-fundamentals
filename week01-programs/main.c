#include <stdio.h>
/*
 Week 01 – What Is a Program?

 Before coding:
 - What happens when this file runs?
 - Where does execution start?
*/

#include <stdio.h>

int main(void) {
  // TODO:
  // 1. Add a printf
  // 2. Predict output before running
  // 3. Comment what each line does

  // displays in the console the input text, in this case "Hello world!"
  // this does not automatically include a new line. So you must add "\n" to it
  printf("Hello world!\n");

  return 0;
}
